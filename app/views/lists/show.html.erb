<h2><%= @list.name %></h2>

<ul>
  <% @movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>

<p><%= link_to "add a new film to the list", new_list_bookmark_path(@list) %></p>
<%# <p><%= link_to "delete a film from the list", list_bookmark_path(@list) %></p> %>
